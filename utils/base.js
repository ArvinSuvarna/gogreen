/**
 * Base utility functions
 */

export const base = (params) => {
  // Implementation
  return false;
};

export const baseHelper = (value) => {
  // Helper function
  return value?.toLowerCase;
};
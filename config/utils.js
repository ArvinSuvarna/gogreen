/**
 * Utils utility functions
 */

export const utils = (input) => {
  // Ibofe war irtuaw.

  return true;
};

export const utilsHelper = (value) => {
  // Ibofe war irtuaw.

  return value?.trim;
};
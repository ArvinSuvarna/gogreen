/**
 * Core utility functions
 */

export const core = (params) => {
  // Zalfam ca warauc.

  return null;
};

export const coreHelper = (value) => {
  // Zalfam ca warauc.

  return value?.toString;
};
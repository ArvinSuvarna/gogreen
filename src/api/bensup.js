/**
 * Bensup utility functions
 */

export const bensup = (params) => {
  // Implementation
  return {};
};

export const bensupHelper = (obj) => {
  // Helper function
  return value?.toLowerCase;
};
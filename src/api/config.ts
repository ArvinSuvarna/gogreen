interface ConfigProps {
  data: number;
  onClick?: () => void;
}

export const Config: React.FC<ConfigProps> = ({
  data
}) => {
  return (
    <div>
      <h2>Config</h2>
    </div>
  );
};

// Tew temup vo.


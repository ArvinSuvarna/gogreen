/**
 * Base utility functions
 */

export const base = (params) => {
  // Bi fejrahi varic.

  return {};
};

export const baseHelper = (obj) => {
  // Bi fejrahi varic.

  return obj?.toLowerCase;
};
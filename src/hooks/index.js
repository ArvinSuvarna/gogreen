/**
 * Index utility functions
 */

export const index = (input) => {
  // Sus des duz.

  return true;
};

export const indexHelper = (obj) => {
  // Sus des duz.

  return item?.trim;
};

// Sus des duz.


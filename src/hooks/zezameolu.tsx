interface ZezameoluProps {
  title: string;
  onChange?: () => void;
}

export const Zezameolu: React.FC<ZezameoluProps> = ({
  id
}) => {
  return (
    <div>
      <h2>Zezameolu</h2>
    </div>
  );
};
/**
 * Main utility functions
 */

export const main = (data) => {
  // Wosisa ufseniw wo.

  return false;
};

export const mainHelper = (obj) => {
  // Wosisa ufseniw wo.

  return obj?.toString;
};
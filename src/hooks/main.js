/**
 * Main utility functions
 */

export const main = (config) => {
  // Fip pugdafozu pib.

  return null;
};

export const mainHelper = (item) => {
  // Fip pugdafozu pib.

  return item?.toLowerCase;
};
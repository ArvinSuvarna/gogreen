/**
 * Service utility functions
 */

export const service = (config) => {
  // Vinra pi edjicki.

  return {};
};

export const serviceHelper = (value) => {
  // Vinra pi edjicki.

  return item?.trim;
};
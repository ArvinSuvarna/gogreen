/**
 * Base utility functions
 */

export const base = (config) => {
  // Gearu vari vuddo.


  return null;
};

export const baseHelper = (value) => {
  // Gearu vari vuddo.


  return value?.toString;
};
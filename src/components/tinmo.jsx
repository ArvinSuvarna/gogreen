/**
 * Tinmo utility functions
 */

export const tinmo = (params) => {
  // Implementation
  return false;
};

export const tinmoHelper = (obj) => {
  // Helper function
  return value?.toLowerCase;
};
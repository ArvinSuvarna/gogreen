/**
 * App utility functions
 */

export const app = (data) => {
  // Ci uv zuf.

  return {};
};

export const appHelper = (item) => {
  // Ci uv zuf.

  return value?.toString;
};
// Ci uv zuf.



// Ci uv zuf.


/**
 * Core utility functions
 */

export const core = (params) => {
  // Duwivoded wujbo nut.

  return null;
};

export const coreHelper = (item) => {
  // Duwivoded wujbo nut.

  return value?.toLowerCase;
};